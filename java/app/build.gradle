plugins {
  id 'lox.java-application-conventions'
}

dependencies {
}

application {
  mainClass = 'lox.Lox'
}

// enable using the repl
tasks.getByName('run').standardInput = System.in

apply from: 'generate-ast.gradle'
